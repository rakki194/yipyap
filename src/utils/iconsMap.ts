// src/utils/iconsMap.ts

import { FolderIcon, SizeIcon, TimeIcon } from "~/icons";

export const captionIconsMap: Record<string, string> = {
  folder: FolderIcon,
  size: SizeIcon,
  time: TimeIcon,
};
